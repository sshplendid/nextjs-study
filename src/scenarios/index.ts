import * as sample  from './sample'
import * as animal  from './animal'
import { TestType } from '../types/mbti'

const scenarios: {[key: string]: TestType } =  {
  sample,
  animal,
}

export default scenarios